{
  "scripts": {
    "dev": "npx concurrently -k -n server,preview -c blue,green \"npm run dev:server\" \"npm run dev:preview\"",
    "dev:server": "cd server && npm run dev",
    "dev:preview": "bin/dev"
  },
  "devDependencies": {
    "@shopify/prettier-plugin-liquid": "^1.9.3",
    "concurrently": "^9.1.2",
    "prettier": "^3.5.3"
  }
}
